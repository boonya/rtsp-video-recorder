language: node_js
node_js:
  - stable
  - 12
  - 10
arch:
  - amd64
os: linux
dist: bionic

install:
  - npm install
before_script:
  - npm run lint:prod
  - npm run test
# script:
#   - npm run build

branches:
  only:
    - master
notifications:
  email: false
