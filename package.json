{
  "name": "rtsp-video-recorder",
  "version": "1.2.1-alpha.1",
  "description": "Provide an API to record rtsp video stream to filesystem.",
  "main": "dist/recorder.js",
  "types": "dist/recorder.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/boonya/rtsp-video-recorder"
  },
  "engines": {
    "node": ">=8",
    "npm": ">=6"
  },
  "scripts": {
    "start": "npm run lint && npm run test && npm run dev",
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "tslint --project tsconfig.json --config tslint.json --format stylish",
    "lint:prod": "tslint --project tsconfig.json --config tslint.prod.json --format stylish"
  },
  "keywords": [
    "rtsp",
    "video",
    "recorder",
    "stream",
    "webcam"
  ],
  "author": "Serhii [boonya] Buinytskyi",
  "license": "MIT",
  "dependencies": {
    "du": "^1.0.0",
    "fs-extra": "^9.0.1",
    "strftime": "^0.10.0"
  },
  "devDependencies": {
    "@types/du": "^1.0.0",
    "@types/fs-extra": "^9.0.1",
    "@types/jest": "^26.0.3",
    "@types/node": "^14.0.14",
    "@types/strftime": "^0.9.2",
    "jest": "^26.1.0",
    "ts-jest": "^26.1.1",
    "tslint": "^6.1.2",
    "tslint-config-standard": "^9.0.0",
    "typescript": "^3.9.6"
  }
}
